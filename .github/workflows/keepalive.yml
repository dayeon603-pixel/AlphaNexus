name: keep-alive
# Render 무료 티어가 15분 idle 시 잠드는 것을 막기 위해 10분마다 /health 핑
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping health
        run: curl -fsS --max-time 60 https://ipscope.onrender.com/health || true
