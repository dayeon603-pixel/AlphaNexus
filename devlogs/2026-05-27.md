# 2026-05-27 — Mirror: NeuroVoice +30 tests (MAE pre-training + EMG adaptation)

*This devlog mirrors activity from the primary NeuroVoice repository.*

## Summary

Two test modules added to NeuroVoice today: total +30 tests.

### `tests/test_pretrain.py` (20 tests) — commit d29e2be6

MAE pre-training loop (`src/training/pretrain.py`) was fully implemented but had
zero test coverage. Added `_ToyDataset` helper that returns `{"neural": (C,W),
"mask": (W,) bool}` without HDF5, then:

- **TestMaskedMSE (6):** unit tests for `_masked_mse` private function
- **TestRunPretrainSmoke (4):** independent 2-step runs for basic sanity
- **TestRunPretrainCheckpoints (5):** class-scoped fixture, `checkpoint_every=3,
  max_steps=6` — verifies both `best_encoder.pt` and `final_encoder.pt` written
- **TestRunPretrainOutput (5):** class-scoped fixture, output shape/finite/determinism,
  decoder reconstruction finite, weights differ from random init

### `tests/integration/test_emg_adapt_pipeline.py` (10 tests) — commit 20ddc35c

End-to-end integration of `GaddyCalibrationDataset → build_calibration_batches →
SessionAdapter.adapt`. No HDF5 needed: `_FakeUtterance(session_id, emg: ndarray[8×T])`
+ `SimpleNamespace._utterances` stub.

- **TestSyntheticCalibrationPipeline (5):** fast/deep adapt complete, rollback restores
  exact weights, output finite, window shapes `(1,8,500)` verified
- **TestAdaptationConvergence (5):** FA loss decreases over 30 steps, temporal loss
  runs, `recommend_strategy` all 5 branches, `n_params fast < deep`, combined loss

## Other projects

- **SPS:** stable; `kaggle_adversarial_sps.ipynb` pending GPU run
- **metaMirage:** complete; manual Kaggle submission steps pending
