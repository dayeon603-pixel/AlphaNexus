# 2026-05-28 — satellite: SPS 262 tests; NeuroVoice 514 tests

## [00:23 UTC] Session — satellite

### Cross-project work
- **SPS**: Fixed cross-test torch/sps stub contamination in  and . All 262 non-torch tests now pass in any pytest collection order.
- **NeuroVoice**: Fixed  select_device stub contamination. Fixed zero-signal IndexError in  median_freq computation. Added 101 new non-torch tests (test_pipeline_types.py 21, test_correction_merge.py 20, test_emg_features.py 25, test_vocabulary_tiers.py 28). NeuroVoice non-torch suite: 514 tests (was 413, +101).

### No new work in this repo this session

---

## [02:16 UTC] Session — satellite: NeuroVoice 631 tests

### Cross-project work
- **NeuroVoice**: 4 new non-torch test files (117 tests). Suite: 631 tests (was 514, +117).

### No new AlphaNexus work this session



---

## [03:10 UTC] Session — satellite: NeuroVoice 744 tests (+113)

### Cross-project work
- **NeuroVoice**: 5 new non-torch test files (113 tests): test_tts_cosyvoice_voice_bank.py (21),
  test_correction_whisper_llm.py (22), test_personalization_federated_on_device.py (21),
  test_emg_calibration_ble.py (37), test_audio_denoiser.py (10) — covers CosyVoiceTTS,
  VoiceBank, Hypothesis, WhisperHypothesisGenerator, CorrectionResult, LLMCorrector,
  FederatedClient, AdapterWeights, OnDeviceAdapter, CalibrationSample/Progress/EMGCalibrator,
  BLE bridge, Denoiser. NeuroVoice non-torch suite: 744 tests (was 631, +113).

### No new work in this repo this session

---

## [04:30 UTC] Session — satellite: NeuroVoice 799 tests (+55)

### Cross-project work
- **NeuroVoice**: 2 new non-torch test files (55 tests): test_semantic_sonar_utils.py (23),
  test_semantic_alignment_confidence.py (32) — covers SONARTextEncoder, SONARTextDecoder,
  SONARProjector, SONARAlignmentLoss, SemanticVector, ConfidenceEstimator with mock-torch
  stub infrastructure. NeuroVoice non-torch suite: 799 tests (was 744, +55).

### No new AlphaNexus work this session

---

## [05:30 UTC] Session — satellite: NeuroVoice 834 tests (+35)

### Cross-project work
- **NeuroVoice**: 2 new non-torch test files (35 tests): test_serving_inference.py (23),
  test_training_finetune_noarch.py (12) — covers StreamingDecoder (src/serving/inference.py)
  and finetune constants/_set_seed/run_finetune signature (src/training/finetune.py).
  NeuroVoice non-torch suite: 834 tests (was 799, +35).

### No new AlphaNexus work this session

---

## [06:20 UTC] Session — satellite: NeuroVoice 891 tests (+57)

### Cross-project work
- **NeuroVoice**: 1 new non-torch test file (57 tests): test_vocabulary_inventory.py —
  covers VocabularyInventory and ConceptEntry (SONAR_DIM, ALIGNMENT_SIMILARITY_THRESHOLD,
  PENDING_MARKER, full CRUD + filtering + coverage_report + save/reload round-trip).
  NeuroVoice non-torch suite: 891 tests (was 834, +57).

### No new AlphaNexus work this session

---

## [07:00 UTC] Session — satellite: NeuroVoice 944 tests (+53)

### Cross-project work
- **NeuroVoice**: 2 new non-torch test files (53 tests):
  test_gaddy_calibration.py (38) — GaddyCalibrationDataset window math/session
  filtering/summary, build_calibration_batches, make_feature_alignment_loss math
  (zero for standardized input, custom targets/weights, 1D/2D auto-promotion),
  make_temporal_consistency_loss statefulness (first=0, identical→0, orthogonal→1.0).
  test_pipeline_noarch.py (15) — NeuroVoicePipeline __all__, mock construction,
  freeze_encoder param freezing, forward chain, load_encoder_weights KeyError guard,
  save/from_checkpoint. NeuroVoice non-torch suite: 944 tests (was 891, +53).

### No new AlphaNexus work this session

---

## [07:30 UTC] Session — satellite: SPS 293 tests (+31)

### Cross-project work
- **SPS**: 1 new non-torch test file (31 tests): `tests/test_utils.py` — covers
  `set_seed`, `cosine_divergence`, `l2_divergence`, `get_divergence_fn`,
  `normalize_directions`, `batch_dot` in `src/sps/utils.py`. Uses numpy-backed
  `_FT` fake-tensor + mocked `F.normalize`/`torch.norm`. SPS non-torch suite:
  293 tests (was 262, +31).

### No new AlphaNexus work this session

---

## [08:00 UTC] Session

### State at Session Start
- AlphaNexus: 0 JavaScript tests (no Jest infrastructure).
- Engine modules without test coverage: `src/engine/math.js`, `risk.js`,
  `signals.js`, `garch.js`, `hmm.js`.

### Work Completed

**Jest infrastructure + 5 test files (104 tests)**

`babel.config.js` + `package.json` update (test script + `jest` + `babel-jest`
devDependencies): enables `npm test` via Babel transpilation of ES module
import/export to CommonJS for Jest.

`tests/engine/math.test.js` (61 tests):
TestMean(4): [1..5]→3, single, negatives, two-element.
TestVariance(3): constant→0, [0..4]→2, single element.
TestStd(3): [0,2,4]→sqrt(8/3), constant→0, non-negative.
TestLogReturns(4): length, ln(1.1), zero, negative sign.
TestSimpleReturns(4): length, 0.1, zero, negative.
TestEMA(4): first element unchanged, length, constant, k=2/(n+1) weighting.
TestNormCDF(6): 0→0.5, 1.96→0.975, symmetry, tails.
TestNormPDF(3): at zero=1/sqrt(2π), symmetry, positive.
TestClamp(5): below lo, above hi, in range, boundaries.
TestCorrelation(5): identical→1, reversed→−1, length mismatch, constant, single.
TestPercentile(5): 50th, 0th, 100th, empty→NaN, 25th interpolation.
TestRolling(2): length=n−w+1, rolling mean.
TestRealizedVariance(3): zeros→0, [1,2,3]→14, [5]→25.
TestSoftplus(3): large x→x, softplus(0)=ln(2), always positive.
TestSigmoid(4): at 0, tails, monotone.
TestSeededRNG(3): reproducible same-seed, range [0,1), seed isolation.

`tests/engine/risk.test.js` (18 tests):
TestNullCases(2): empty returns, single-element returns → nullMetrics.
TestUptrend(12): annRet/annVol/sharpe positive, mdd=0 for strictly increasing
  prices, var99≤var95, cvar95≤var95, omega=999 (no losses→capped Infinity),
  winRate=1.0, nObs, drawdownSeries length, annVolPct, mddPct.
TestRollingSharpe(4): output length, first window−1 NaN, positive/negative
  for corresponding constant return series.

`tests/engine/signals.test.js` (9 tests):
TestComputeRSI(5): length, [0,100] range, all-up→100, all-down→0, short→50s.
TestTrueATR(4): length n−period+1, non-negative, zero range→0, constant 10→10.

`tests/engine/garch.test.js` (10 tests):
TestGarchTermStructure(6): stepForecasts length=horizonDays, all positive,
  horizonVol/annualizedVol positive, phi=0→longRunVar, phi=1→horizonDays×h0.
TestGJRGarchMC(4): nPaths, length horizonDays+1, starts at S0, all prices positive.

`tests/engine/hmm.test.js` (6 tests):
TestRegimeForecast(6): sums to 1, steps=0→initial dist, 1-step hand calculation
  [0.695], long horizon→stationary π_B=2/3, result in [0,1], deterministic.

### Results / Observations
- AlphaNexus engine test suite: 104 tests (was 0).
- All public deterministic engine functions covered. Public stubs (tsmom, fitOU,
  ouSignal, fitHMM, kellyFraction, icWeightedComposite) intentionally not tested
  — return zeros/defaults; proprietary logic excluded from public repo.

### Next Steps
**AlphaNexus:** `npm install && npm test` to run suite (requires Node + npm).
  Next: tests for `src/data/fetcher.js` mock layer if needed.

---

## [09:00 UTC] Session — satellite: rootslink 117 algorithm tests (new)

### Cross-project work
- **rootslink**: pytest infrastructure + 4 test files (117 tests):
  `tests/test_opportunity_fit.py` (36), `tests/test_mentor_match.py` (28),
  `tests/test_brain_drain_risk.py` (29), `tests/test_retention_priority.py` (24) —
  covers OpportunityFit, MentorMatch, BrainDrainRisk, RetentionPriority algorithms
  with SimpleNamespace duck-typing. rootslink algorithm suite: 117 tests (was 0).

### No new AlphaNexus work this session
