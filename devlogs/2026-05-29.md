# 2026-05-29 — AlphaNexus: forecast.test.js 20 tests (+20); satellite SPS/NeuroVoice/rootslink

## [00:18 UTC] Session

### State at Session Start
- AlphaNexus: 118 JS tests (from 2026-05-28 session).
- `src/engine/forecast.js` had zero test coverage.

### Work Completed

**`tests/engine/forecast.test.js` (20 tests, new)**

Full coverage for `multiTimeframeForecast` — the public entry point of the
multi-timeframe forecasting engine.

Fixture design: `mockGarchBull`/`mockHmmBull`/`mockHmmBear` with proper fields
matching what `regimeForecast(hmm, horizonSteps)` expects — specifically `A`
(transition matrix) and `bullProb` (initial distribution) rather than the
incorrect `transitionMatrix` field tried initially.

multiTimeframeForecast — output structure (5):
  returns array, 3 core timeframes (no intraday), labels ['1 DAY','1 WEEK','1 MONTH'],
  all required keys present, horizonDays numeric and [1, 5, 22].

multiTimeframeForecast — intraday inclusion (5):
  adds '1 MIN' when ≥30 minute prices; adds '1 HOUR' when ≥20 hourly prices;
  adds both when both available (→ 5 results); skips '1 MIN' when <30 prices;
  skips '1 HOUR' when <20 prices.

multiTimeframeForecast — value ranges (6):
  confidence ∈ [0.50, 0.65]; signal ∈ [-1, 1]; ci68 lo ≤ predictedPct ≤ ci68 hi;
  ci95 lo ≤ ci68 lo and ci68 hi ≤ ci95 hi; positive horizonVol/annualizedVol;
  direction ∈ {BUY, SELL, HOLD}.

multiTimeframeForecast — bull vs bear regime (4):
  bull predictedPct > bear predictedPct on '1 DAY'; bull direction = BUY;
  bear direction = SELL; longer horizons have larger CI95 width than shorter.

AlphaNexus JS suite: 138 tests (was 118, +20).

### Cross-project work
- **SPS**: `tests/test_adversarial_gap_integration_noarch.py` (50 tests). SPS suite: 395 tests.
- **NeuroVoice**: `tests/test_train_vocoder_noarch.py` (25 tests). NeuroVoice suite: 1106 tests.
- **rootslink**: `tests/test_score_reasons.py` (22 tests). rootslink suite: 158 tests.

### Results / Observations
- CI nesting invariant (ci95 ⊇ ci68 ⊇ {predicted}) held for all three core timeframes
  with both bull and bear regimes. This is a structural guarantee from the GARCH term
  structure (σ68 = σ, σ95 = 1.96σ).
- Longer horizon → wider CI width confirmed via 1 DAY vs 1 MONTH comparison. GARCH
  cumulative variance scales with horizon steps, not just per-step vol.
- Bull regime correctly produces BUY (mockSignal = clamp((0.75...- 0.5) × 2, -1, 1) ≈
  +0.5 → positive → BUY). Bear regime produces SELL (bullProb=0.25, mockSignal ≈ -0.5).

### Hypotheses
- H-forecast1: The confidence range [0.50, 0.65] is a design choice — the public version
  uses `0.50 + |mockSignal| × 0.15`, which is intentionally conservative (50-65%)
  to avoid implying false precision for the mock signal.

### Next Steps
**AlphaNexus:** Consider widget UI tests for the DOM rendering path (browser-testable
  via jsdom or Playwright). The `src/widget/widget.js` has no test coverage.

---

## [~continuation] Session — satellite

### Cross-project work (no new AlphaNexus work this session)
- **SPS**: `tests/test_stats_extended_noarch.py` (50 tests, new) — dedicated unit tests
  for all 7 functions in src/sps/stats.py. SPS suite: 445 tests (was 395, +50).
- **NeuroVoice**: `tests/test_eval_metrics_noarch.py` (30 tests, new) — evaluation/metrics
  noarch. NeuroVoice suite: 1136 tests (was 1106, +30).
- **rootslink**: `tests/test_schemas_noarch.py` (42 tests, new) — Pydantic v2 schema
  validation. rootslink suite: 200 tests (was 158, +42).

AlphaNexus JS suite: 138 tests (unchanged — widget.js tests remain as next step).

---

## [~continuation] Session — satellite

### Cross-project work
- **NeuroVoice**: `test_vocabulary_tiers_noarch.py` (58 tests) + `test_tier1_template_noarch.py`
  (51 tests). NeuroVoice suite: 1245 tests (was 1136, +109).

---

## [~continuation] Session

### State at Session Start
- AlphaNexus: 138 JS tests (from prior sessions).
- `src/widget/widget.js` (renderWidget, destroyWidget) had zero test coverage.
  Pure DOM manipulation, no React.

### Work Completed

**`tests/widget/widget.test.js` (26 tests, new)**

Full DOM test coverage for `renderWidget` and `destroyWidget` from `src/widget/widget.js`.

Environment: `@jest-environment jsdom` docblock per-test-file override.
`jest-environment-jsdom` added to package.json devDependencies.

Shell creation (6): `.an-widget` created inside root; `#an-close` button exists;
  `#an-toggle` button exists; `#an-refresh` button exists; calling renderWidget
  twice does not duplicate `.an-widget`; `#an-body` exists.

Loading state (3): ticker text in `#an-ticker-row`; `.an-loading` element present;
  `.an-table` absent.

Error state (3): ticker text in `#an-ticker-row`; `.an-error` element contains error text;
  `.an-table` absent.

Ready state (9): `.an-table` exists; 3 `tr.an-row` forecast rows (from MOCK_DATA);
  BUY row has `↑`; SELL row has `↓`; HOLD row has `→`; `.an-metrics-row` elements
  present; `.an-footer` exists; ticker text in `#an-ticker-row`; price formatted with `$`.

Controls (3): close button sets `root.style.display = 'none'`; toggle button changes
  `#an-body` visibility (state-agnostic: toggle twice, verify restore); refresh button
  calls the `onRefresh` callback.

destroyWidget (2): clears root innerHTML to `''`; `.an-widget` no longer present.

AlphaNexus JS suite: 164 tests (was 138, +26).

### Cross-project work
- **NeuroVoice**: `tests/test_articulatory_noarch.py` (25 tests) — articulatory constants,
  ArticulatoryInverter defaults, _normalise static method. NeuroVoice suite: 1270 tests.
- **rootslink**: `tests/test_api_routes_noarch.py` (30 tests) — FastAPI TestClient route
  tests for health, info, user CRUD, interaction logging, overview stats, match 404.
  rootslink suite: 230 tests.

### Results / Observations
- `_minimised` is module-level state in widget.js — shared across all tests in the same
  Jest file (babel-jest transforms ES modules to CJS, sharing one module instance).
  Toggle test written state-agnostically: read initial state, click, verify change, click
  again, verify restore.
- `makeDraggable` in buildShell attaches `mousedown` listeners to `.an-header` — jsdom
  supports addEventListener so no errors, and no mousedown events are fired in tests.
- `destroyWidget` sets `root.innerHTML = ''` directly — simple and effective, leaves
  root in clean state for re-injection.

### Hypotheses
- H-widget1: The no-duplicate-shell invariant (`!root.querySelector('.an-widget')`) is
  key for extension use: `chrome.runtime.onMessage` triggers re-render on every price
  update. Without the guard, each message would recreate the entire DOM tree,
  destroying the dragged position and resetting `_minimised`.

### Next Steps
**AlphaNexus:** All major modules now have test coverage. Next: options chain visualization
  tests (`src/engine/options.js`) if that file exists.

---

## [04:18 UTC] Session — satellite

### Cross-project work
- **SPS**: `tests/test_utils_noarch.py` (19 tests). SPS suite: 464 tests (was 445, +19).
- **NeuroVoice**: `tests/test_lang_registry_noarch.py` (72 tests). NeuroVoice suite: 1342 tests (was 1270, +72).

### No new AlphaNexus work this session
AlphaNexus suite: 164 tests. Satellite-only this session.

---

## [~05:30 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: audio module noarch tests — preprocessing (28 tests), VAD (22 tests),
  beamformer (25 tests). NeuroVoice suite: 1417 tests (was 1342, +75).

### No new AlphaNexus work this session
AlphaNexus suite: 164 tests. Satellite-only this session.

---

## [06:25 UTC] Session — satellite

### Cross-project work
- **NeuroVoice**: 6 new noarch test suites (+167 tests) — correction/merge (20),
  lexicon_rescorer (52), emg/features (24), emg/calibration (29), pipeline/types (24),
  tts/voice_selector (18). Also fixed Python 3.11 `slots=True` compatibility in
  audio_preprocessing/vad/beamformer test loaders. NeuroVoice suite: 1584 tests
  (was 1417, +167).

### No new AlphaNexus work this session
AlphaNexus suite: 164 tests. All engine modules tested; background.js requires
Chrome extension environment (untestable noarch). Satellite-only this session.
