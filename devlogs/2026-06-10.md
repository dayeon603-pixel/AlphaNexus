# 2026-06-10 — Mirror: SPS R105 + NeuroVoice R105

*This devlog mirrors activity from the primary SPS and NeuroVoice repositories.*

## [00:00 UTC] Satellite — R105

- SPS R105: `tests/test_jacobian_jvp_behavioral_noarch.py` (+30, suite 5,488 → 5,518).
  Behavioral noarch tests for `sps/core.py` — `AdversarialSPSReport.summary()` (3 label branches:
  near-adversarial/moderate/LARGE, separator, float values, n_samples) and
  `RelativeSPSReport.summary()` (3 label branches: MORE stable/≈ T_arb/LESS stable, CI YES/no flag,
  float values, loo_t stable) and field storage contracts (both dataclasses, stored values,
  loo_emb dict, config=None defaults, n_samples int).
  All 30 tests pass (0.07 s, isolated importlib load).

- NeuroVoice R105: `tests/test_pipeline_source_2_noarch.py` (+30, suite 7,191 → 7,221).
  2nd source-expression pass for `src/models/pipeline.py` — module imports (__future__/pathlib/
  typing/loguru/.encoder/.decoder), __init__ param type annotations (EncoderConfig/DecoderConfig/
  freeze_encoder: bool = False/neural: torch.Tensor), load_encoder_weights full signature + exact
  KeyError message + tuple assignment + guards, save signature, @classmethod + forward ref return
  type, from_checkpoint full body (cls() call, all 3 kwargs, logger loaded info, return pipeline,
  path positional arg, -> torch.Tensor: + -> None: annotations).
  All 30 tests pass (0.07 s, raw source read --noconftest).

## Status

| Repo | Suite | R105 delta |
|------|-------|------------|
| SPS | 5,518 | +30 |
| NeuroVoice | 7,221 | +30 |
| AlphaNexus | satellite-only | — |
| RootsLink | satellite-only | — |
| metaMirage | post-deadline | — |
| **Combined** | **12,739** | **+60** |

---

## [04:27 UTC] Satellite — R106

- SPS R106: `tests/test_core_estimator_return_residual_noarch.py` (+30, suite 5,518 → 5,548).
  Source residuals for `AdversarialSPSEstimator.estimate()` return block (dict reads
  mean_sensitivity/std_sensitivity/n_samples for emb+adv, adv_sens_ci bootstrap, loo_adv
  spectral_gap, return AdversarialSPSReport( keyword, sps_emb=/sps_adv= kwargs) and constructor
  kwarg tail (adv_gap_ratio=, mean_sens_emb=, std_sens_emb=, mean_sens_adv=, std_sens_adv=,
  n_samples=, emb_sens_ci=, adv_sens_ci=, loo_emb=, loo_adv=), plus RelativeSPSEstimator
  residuals (t/arb dict reads, emb_std_s=/emb_n=/arb_n= kwargs, sens_ci_t bootstrap+seed).
  All 30 tests pass (0.07 s).

- NeuroVoice R106: `tests/test_pipeline_behavioral_noarch.py` (+30, suite 7,221 → 7,251).
  Behavioral integration tests for NeuroVoicePipeline — __init__ stores encoder/decoder with
  correct configs, freeze_encoder=True sets requires_grad_(False) on all params, logger called
  with "frozen"; forward passes neural to encoder, encoder output to decoder, tone_cond
  optional default None; load_encoder_weights raises KeyError on missing key, calls
  encoder.load_state_dict(strict=True); save produces 4-key dict; from_checkpoint returns
  NeuroVoicePipeline with checkpoint's encoder_config.
  All 30 tests pass (0.17 s, importlib + _StubEncoder/_StubDecoder/_MockParam).

## Status

| Repo | Suite | R106 delta |
|------|-------|------------|
| SPS | 5,548 | +30 |
| NeuroVoice | 7,251 | +30 |
| AlphaNexus | satellite-only | — |
| RootsLink | satellite-only | — |
| metaMirage | post-deadline | — |
| **Combined** | **12,799** | **+60** |
