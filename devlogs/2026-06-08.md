# AlphaNexus Devlog — 2026-06-08

## [03:04 UTC] Session — satellite R74

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_adversarial_sps_results_analysis_noarch.py` (+30 noarch tests, suite→4588). experiments/adversarial_sps_results_analysis.py source-expression pass: load_results/format_results_table/verify_hypotheses/build_summary bodies + module constants.
- **NeuroVoice**: `tests/test_code_switch_source_noarch.py` (+30 noarch tests, suite→6291). src/multilingual/code_switch.py source-expression pass: CodeSwitchResult dataclass, CodeSwitchDetector.__init__/load/detect short-path and sliding-window bodies, _predict_language* methods.

## [04:04 UTC] Session — satellite R75

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_cross_arch_adversarial_noarch.py` (+30 noarch tests, suite→4618). experiments/cross_arch_adversarial.py source-expression pass: TEST_SENTENCES/ARCHITECTURES constants, prepare_batches body, collect_spectral_gaps body, ArchResult NamedTuple, validate_hypotheses body, serialise_results body.
- **NeuroVoice**: `tests/test_decoder_heads_source_noarch.py` (+30 noarch tests, suite→6321). src/multilingual/decoder_heads.py source-expression pass: GeneratedOutput dataclass fields, DecoderHead.__init__/load/is_loaded/generate, DecoderHeadManager.__init__/get_head LRU eviction body.

## [05:07 UTC] Session — satellite R76

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_estimate_sps_source_noarch.py` (+30 noarch tests, suite→4648).
- **NeuroVoice**: `tests/test_lang_pack_manager_source_noarch.py` (+30 noarch tests, suite→6351).

## [06:10 UTC] Session — satellite R77

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_jacobian_source_noarch.py` (+30 noarch tests, suite→4678). SpectralGapResult: @dataclass(slots=True)/restricted_norm:torch.Tensor/full_spectral_norm:torch.Tensor/normalized_gap:torch.Tensor/mean_restricted_norm:float=field(init=False)/mean_full_norm:float=field(init=False)/mean_gap:float=field(init=False)/__post_init__: self.mean_restricted_norm=self.restricted_norm.mean().item()/self.mean_full_norm=self.full_spectral_norm.mean().item()/self.mean_gap=self.normalized_gap.mean().item(). directional_derivative_norm: v_magnitude=v.flatten(start_dim=1).norm(dim=1)/v_unit=normalize_directions(v)/_,jvp_val=torch.autograd.functional.jvp(scalar_fn,x,v_unit,create_graph=False)/norms=jvp_val.flatten(start_dim=1).norm(dim=1)/return torch.where(v_magnitude>eps,norms,torch.zeros_like(norms)). restricted_operator_norm: B,K,seq_len,hidden=directions.shape/norms_per_dir:list[torch.Tensor]=[]/stacked=torch.stack(norms_per_dir,dim=1)/return stacked.max(dim=1).values. adversarial_worst_direction: worst_k=stacked.argmax(dim=1). full_spectral_norm: max_norm=torch.zeros(B,device=device)/v=torch.randn(B,seq_len,hidden,device=device)/max_norm=torch.maximum(max_norm,jvp_norm). spectral_gap: x=x.detach().requires_grad_(True)/rest_norm=restricted_operator_norm/full_norm=full_spectral_norm/normalized=(1.0-rest_norm/full_norm.clamp(min=eps)).clamp(0.0,1.0). verify_spectral_gap_bound: lower=math.exp(-(1.0-mean_gap)*lipschitz_constant)/"improvement_factor":lower/max(theorem3,1e-12). ForwardFn=Callable[[torch.Tensor],torch.Tensor].
- **NeuroVoice**: `tests/test_tone_conditioner_source_noarch.py` (+30 noarch tests, suite→6381). Module: from .phoneme_articulatory import ToneContourSegment,ARTICULATORY_DIM/__all__=["ToneConditioner"]/class ToneConditioner:/self.art_dim=art_dim. expand_segment: if n_frames<=0:/return np.zeros((0,self.art_dim),dtype=np.float32)/vecs=segment.vectors()/durs=segment.durations()/n_seg=len(vecs)/out=np.empty((n_frames,self.art_dim),dtype=np.float32)/frame_start=0/for i,(vec,dur) in enumerate(zip(vecs,durs)):/if i==n_seg-1:/frame_end=n_frames/else: frame_end=frame_start+round(dur*n_frames)/frame_end=min(frame_end,n_frames)/if frame_end>frame_start:/out[frame_start:frame_end]=vec.features.astype(np.float32)/frame_start=frame_end/return out. build_conditioning: if len(segments)!=len(syllable_spans):/out=np.zeros((T_art,self.art_dim),dtype=np.float32)/for seg,(start,end) in zip/if start<0 or end>T_art or start>=end:/if seg is None:/n_frames=end-start/out[start:end]=self.expand_segment(seg,n_frames). to_tensor: import torch/t=torch.from_numpy(np.ascontiguousarray(cond,dtype=np.float32))/t=t.unsqueeze(0).

## [07:15 UTC] Session — satellite R78

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_transformations_source_noarch.py` (+30 noarch tests, suite→4708). EmbeddingPerturbationConfig field annotations: n_directions:int=8/projection_noise_scale:float=0.02/use_synonym_directions:bool=True. EmbeddingPerturbationFamily init: self.config=config or EmbeddingPerturbationConfig(). sample body: K=directions.size(1)/idx randint/v gather/alpha_exp view/perturbed add. semantic_directions: K=self.config.n_directions/use_synonym guard/two branch returns. _synonym_directions: delta subtraction/noise_scale read/randn_like addition/cat return. _random_orthogonal_directions: flat_dim/randn raw/zeros_like ortho. SynonymSubstitutionFamily: if not synonym_map init guard/perturbed_ids clone/n_substituted=0/randperm positions/valid mask/magnitudes*valid.float(). AdversarialEmbeddingFamily: forward_fn assignment/_t_emb construction/late jacobian import/v_star normalize-view.
- **NeuroVoice**: `tests/test_vocabulary_inventory_source_noarch.py` (+30 noarch tests, suite→6411). Constants: SONAR_DIM:int=1024/ALIGNMENT_SIMILARITY_THRESHOLD:float=0.75/PENDING_MARKER. ConceptEntry: concept_id/concept_name/translations/sonar_vector field(default=None,repr=False). get_primary/from_dict/to_dict bodies. VocabularyInventory.__init__/load/build_reverse_index/find_concept_by_text/coverage_report key expressions.

## [08:06 UTC] Session — satellite R79

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_core_relative_sps_source_noarch.py` (+30 noarch tests, suite→4738). RelativeSPSReport.summary: "T MORE stable than T_arb"/"T ≈ T_arb"/"T LESS stable than T_arb" interp strings/rsps_point>1.05/rsps_point>0.95/sig_label conditional/Relative SPS Analysis header/SPS_T line/rSPS point estimate/rSPS 95% CI delta-method. RelativeSPSEstimator.__init__: self.t_estimator=t_estimator/self.arb_estimator=arb_estimator/self.config=config or t_estimator.config. _collect_sensitivities: all_sens list init/input_ids to device/"embeddings" guard/append cpu/torch.cat.tolist return. estimate: bootstrap_ci+delta_method_rsps_ci+loo_spectral_gap import/t/arb estimate(iter(batches))/sps_t/sps_arb dict gets/delta_method_rsps_ci call/rsps_ci tuple/rsps_ci_excludes_one/t_sens_list collect.
- **NeuroVoice**: `tests/test_lang_registry_source_noarch.py` (+30 noarch tests, suite→6441). QualityTier(IntEnum)/VALIDATED=1/AUTO=2/COMMUNITY=3. @dataclasses.dataclass(slots=True,frozen=True)/code:str/bcp47:str/tier:QualityTier/has_impaired_data:bool/seamless_s2s:bool=False. LANGUAGE_REGISTRY dict annotation/"ko":LanguageInfo(code/bcp47/name/tier=VALIDATED/has_impaired_data=True/family="African"/decoder_size_mb:float/tts_size_mb:float/seamless_s2t=True/sonar_supported=True. get_mvp/get_by_tier/get_by_family/get_seamless_s2s list comp bodies/total=0.0 init/sorted(LANGUAGE_REGISTRY.keys())/sorted({lang.family...}).

## [09:06 UTC] Session — satellite R80

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_sps_init_source_noarch.py` (+30 noarch tests, suite→4768). sps/__init__.py source-expression pass: from sps.core import/from sps.jacobian import/from sps.metrics import/from sps.transformations import (AdversarialEmbeddingConfig/SynonymSubstitutionConfig/TransformationFamily)/from sps.stats import (permutation_test_gap_diff/compare_architecture_cis/delta_method_rsps_ci)/from sps.utils import (cosine_divergence/get_divergence_fn/normalize_directions)/__all__ entries (# core/# stats comments/"AdversarialSPSEstimator"/"permutation_test_gap_diff").
- **NeuroVoice**: `tests/test_emg_calibration_source_noarch.py` (+30 noarch tests, suite→6471). src/emg/calibration.py source-expression pass: MIN_REPS_PER_WORD/RECOMMENDED_REPS constants/CalibrationSample 4-field dataclass/CalibrationProgress fields+properties (is_minimum_met/is_recommended_met/total_samples/next_word/status_message)/EMGCalibrator.__init__/add_sample/_save_meta/get_training_data bodies.

## [10:08 UTC] Session — satellite R81

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_sps_core_spsconfig_sse_estimate_source_noarch.py` (+30 noarch tests, suite→4798). sps/core.py new-territory source-expression pass: SPSConfig field annotations (class declaration, pydantic import block, epsilon/n_data_samples/m_transforms Field annotations with gt/ge/description args, divergence DivergenceType default, device str "cpu", seed int 42, batch_size Field ge=1, @field_validator("device") decorator). StructuredSensitivityEstimator.estimate() body (raw file; @torch.no_grad() decorated): 3 to(self.device) assignments, B=embeddings.size(0), fx=_forward call, max_ratios zeros init, for _ in range(m) loop, valid cx>1e-12 mask, div=divergence_fn(fx,ftx), torch.maximum update. _forward body (inspect.getsource): hasattr last_hidden_state check, CLS-token [:, 0, :] return, isinstance Tensor check, TypeError raise. _get_embeddings body: get_input_embeddings getattr, attr loop ("roberta"/"bert"/"transformer"/"model"), embeddings/word_embeddings getattr. build_sps_estimator factory: config or SPSConfig(), get_divergence_fn(config.divergence).
- **NeuroVoice**: `tests/test_beam_decoder_source_noarch.py` (+30 noarch tests, suite→6501). src/emg/beam_decoder.py source-expression pass: module constants (CTC_BLANK_ID/DEFAULT_BEAM_WIDTH/DEFAULT_LM_WEIGHT/NEG_INF), VocabularyTrie.__init__ (words set comp/_marisa=None/dict_trie typed init), _build_dict_trie (setdefault loop/terminal marker), is_complete_word return, BeamHypothesis dataclass slots=True + token_ids/ctc_score/combined_score/confidence/n_frames fields, _BeamState p_blank/p_nonblank NEG_INF defaults + total_log_prob body, _log_add math.log1p formula, MultilingualBeamDecoder.__init__ 4 assignments, decode() lm.get/blank_lp/new_p_blank formula/confidence softmax, _prefix_to_text chars list, _is_word_boundary space return.

## [12:06 UTC] Session — satellite R83

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_stats_spectral_report_compare_perm_source_noarch.py` (+30 noarch tests, suite→4858). sps/stats.py 2nd source pass: loo_batch_spectral_gap body (n/total/loo_means/grand_mean/worst_idx lambda/n_batches+grand_mean+worst_batch return keys), spectral_gap_report body (bootstrap_ci+loo_spectral_gap delegation, loo.get fallbacks, ci_width round+n len return keys), compare_architecture_cis body (reports dict, arch-gaps loop, spectral_gap_report delegation, ranking sorted reverse=True, reports["ranking"]), permutation_test_gap_diff body (pooled concat, obs_diff, rng.shuffle, perm_diff formula, p_value=(extreme+1)/(n_perm+1)).
- **NeuroVoice**: `tests/test_emg_preprocessing_source_noarch.py` (+30 noarch tests, suite→6561). src/emg/preprocessing.py source-expression pass: constants (SAMPLE_RATE/BANDPASS_LOW/NOTCH_FREQ/ONSET_ENERGY_THRESHOLD), EMGSegment/SpeechOnset dataclasses, EMGPreprocessor.__init__/bandpass_filter/notch_filter/normalize/process/segment bodies, ContinuousEMGBuffer.__init__/write/_drain_windows bodies, detect_speech_onset (frame_samples/baseline_rms formulas).

## [13:04 UTC] Session — satellite R84

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_metrics_source_noarch.py` (+30 noarch tests, suite→4888). sps/metrics.py source-expression pass: SPSReport fields (sps/mean_sensitivity/std_sensitivity/n_samples/relative_sps/spectral_gap_mean/layerwise_profile/config), summary() interpretation strings (>1 semantic stable/<1 FAILURE), relative_sps() body (eps guard/ValueError/division return), estimate_arbitrary_sps() (late import/n_directions=m_transforms/use_synonym_directions=False/get_embedding_layer/build_sps_estimator/estimate/return_full conditional), LayerwiseSPSAnalyzer (4 init assignments/_find_layers, profile list/enumerate loop/append, captured dict/register_forward_hook/handle.remove).
- **NeuroVoice**: `tests/test_ble_bridge_source_noarch.py` (+30 noarch tests, suite→6591). src/emg/ble_bridge.py source-expression pass: base64/numpy imports/__all__ 4 entries/segment_to_decode_frame body/decode_response_articulatory body/decode_response_audio body/make_batch_frame list comp/3 function defs.

## [14:07 UTC] Session — satellite R85

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_stats_delta_method_bootstrap_source_noarch.py` (+30 noarch tests, suite→4918). sps/stats.py third source pass: delta_method_rsps_ci body (z-score alpha branches/log_rsps formula/var_log formula/se_log sqrt/rsps_point/rsps_lo/rsps_hi exp expressions/return round triple), bootstrap_ci body (n/zero-guard/rng/boot_means sorted/rng.choices/lo_idx/hi_idx/return round triple), loo_spectral_gap body (n<3 guard/early return/total sum/loo_means list-comp/stable threshold/loo_range key).
- **NeuroVoice**: `tests/test_lexicon_rescorer_source_noarch.py` (+30 noarch tests, suite→6621). src/correction/lexicon_rescorer.py source-expression pass: levenshtein two-row DP body, build_lexicon_from_transcripts Counter loops, LexiconRescorer.__init__ __slots__/guards/assignments, from_counts total+log_probs, snap_word guards+score formula, rescore_sentence/rescore_batch.

## [15:07 UTC] Session — satellite R86

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_cross_arch_adversarial_source_noarch.py` (+30 noarch tests, suite→4948). experiments/cross_arch_adversarial.py new-territory source pass: run_architecture() model loading (AutoTokenizer/AutoModel pretrained/eval/to device/get_input_embeddings/prepare_batches/SPSConfig/use_synonyms bool/epsilon kwarg), family construction (EmbeddingPerturbationConfig/EmbeddingPerturbationFamily/AdversarialEmbeddingConfig/AdversarialEmbeddingFamily/build_adversarial_sps_estimator/estimator.estimate/collect_spectral_gaps/spectral_gap_report/return ArchResult), print+main (emb_sens_ci unpack/gap_report row/compare_architecture_cis/ranking/permutation_test_gap_diff/sig p_value/set_seed/sentences construction/all_batch_means list comp/loo_batch_spectral_gap).
- **NeuroVoice**: `tests/test_correction_merge_source_noarch.py` (+30 noarch tests, suite→6651). src/correction/merge.py source-expression pass: module constants (DEFAULT_PRIMARY_WEIGHT=0.6/DEFAULT_MERGE_THRESHOLD=0.7)/function signature (NDArray params/defaults/return type tuple)/early-return path (below-threshold guard/logger.debug/return primary,primary_confidence)/weight computation (total_conf/zero guard/w_primary ratio/w_primary bias formula)/blend+normalize (w_correction complement/merged blend/linalg.norm/norm>0 guard/merged/norm/merged_confidence formula/return merged,merged_confidence).

## [16:05 UTC] Session — satellite R87

No new AlphaNexus R&D this session (satellite-only).
- **SPS**: `tests/test_bootstrap_sign_flip_main_loo_source_noarch.py` (+30 noarch tests, suite→4978). experiments/bootstrap_sign_flip.py 2nd source pass: permutation_p full body (r_obs=abs(pearson_r(xs,ys))/ys_perm=list(ys)/extreme=0/for _ in range(n_perm)/abs guard/extreme+=1/return round((extreme+1)/(n_perm+1),4)), bootstrap_correlation_ci residual (hi index formula/return round triple/boot_rs.append(pearson_r(bx,by))), loo_correlation return dict (n/point_r/loo_rs list-comp/loo_min/loo_max/sign_stable keys/analyse permutation_p+loo calls), main() body (--data/--n-boot/--seed args/os.path.exists guard/json.load/raw["profiles"]/INLINE_DATA["models"]/nb,sd unpack/confirmed list comp).
- **NeuroVoice**: `tests/test_whisper_hypotheses_source_noarch.py` (+30 noarch tests, suite→6681). src/correction/whisper_hypotheses.py source-expression pass: Hypothesis dataclass (slots=True/text:str/score:float log-prob), WhisperHypothesisGenerator.__init__ (5 self assignments/_model=None/_processor=None/default model name), load() (WhisperForConditionalGeneration+WhisperProcessor import/from_pretrained calls/eval/logger.info), generate() (model-None guard/RuntimeError/torch import/processor sampling_rate=16_000/no_grad/num_beams/num_return_sequences/hypotheses typed list/sort reverse=True).
